<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/profile">

    <fragment
        android:id="@+id/playList"
        android:name="hcmute.edu.vn.spotifyclone.PlayList"
        android:label="fragment_play_list"
        tools:layout="@layout/fragment_play_list" />
    <fragment
        android:id="@+id/profile"
        android:name="hcmute.edu.vn.spotifyclone.Profile"
        android:label="fragment_profile"
        tools:layout="@layout/fragment_profile" >
        <action
            android:id="@+id/action_profile_to_playList"
            app:destination="@id/playList" />
    </fragment>
    <fragment
        android:id="@+id/search2"
        android:name="hcmute.edu.vn.spotifyclone.Search"
        android:label="fragment_search"
        tools:layout="@layout/fragment_search" />
</navigation>